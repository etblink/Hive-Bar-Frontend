import { AbstractUniverseLog } from "./AbstractUniverseLog";
import { LogEngine } from "./LogEngine";
import { Properties } from "./Properties";
import { UniverseLog } from "./UniverseLog";
export declare class BasicTaggedUniverseLog extends AbstractUniverseLog implements UniverseLog {
    constructor(propsOrEngine: Properties | LogEngine);
    tag(tag: string): BasicTaggedUniverseLog;
}
