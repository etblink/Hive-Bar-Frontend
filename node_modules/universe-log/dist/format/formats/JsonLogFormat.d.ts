import { LogMetadata } from "../../config/LogMetadata";
import { LogMessage } from "../LogMessage";
export declare class JsonLogFormat {
    static FORMAT_NAME: string;
    static FORMAT_NAME_PRETTY: string;
    private pretty;
    constructor(pretty: boolean);
    format(msg: LogMessage, metadata: LogMetadata): string;
    getName(): string;
}
