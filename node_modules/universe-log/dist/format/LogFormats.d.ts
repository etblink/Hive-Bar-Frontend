import { LogFormat } from "./LogFormat";
export declare namespace LogFormats {
    interface Formats {
        json: LogFormat;
        json_pretty: LogFormat;
        oneline: LogFormat;
    }
    type Format = keyof Formats;
    const FORMATS: Formats;
    const KEYS: {
        [x in Format]: Format;
    };
    const DEFAULT_FORMAT: LogFormat;
    function valueOf(name: string): LogFormat;
}
