import { LogLevel } from "../config/LogLevel";
import { LogMessage } from "../format/LogMessage";
export declare class ParseLogMsg {
    static parse(level: LogLevel, elems: any[]): LogMessage;
    private static basicLogMsg;
    private static parseElem;
    private static parseString;
    private static parseError;
    private static errorToObj;
    private static parseObject;
    private static parseOther;
}
