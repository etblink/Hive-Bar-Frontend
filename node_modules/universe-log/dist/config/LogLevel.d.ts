export declare enum LogLevel {
    error = "error",
    warn = "warn",
    info = "info",
    http = "http",
    verbose = "verbose",
    debug = "debug",
    silly = "silly"
}
export declare namespace LogLevel {
    const LEVELS_VALUES: {
        [x: string]: number;
    };
    const LEVELS_BY_NAME: {
        [x: string]: LogLevel;
    };
    const DEFAULT_LEVEL: LogLevel;
    function valueOf(name: string): LogLevel;
    function levelToValue(name: string): number;
    function isLessOrEquallyVerbose(args: {
        level: LogLevel;
        threshold: LogLevel;
    }): boolean;
    function moreVerbose(a: LogLevel, b: LogLevel): LogLevel;
    function lessVerbose(a: LogLevel, b: LogLevel): LogLevel;
}
