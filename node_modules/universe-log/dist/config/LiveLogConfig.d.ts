import { LogFormat } from "../format/LogFormat";
import { LogLevel } from "./LogLevel";
import { LogMetadata } from "./LogMetadata";
export declare class LiveLogConfig {
    private levelEvaluationEnvNames;
    private level;
    private defaultFormat;
    private format;
    private metadata;
    private fallbackLog;
    private nextReevaluateTimestampMs;
    constructor(params: {
        levelEvaluationEnvNames: string[];
        fallbackLog: (msg: string) => void;
        defaultFormat: LogFormat;
    });
    setLevelEvaluationEnvNames(levelEvaluationEnvNames: string[]): void;
    getLevel(): LogLevel;
    getFormat(): LogFormat;
    getMetadata(): LogMetadata;
    evaluateIfRequired(): void;
    private evaluate;
    private evaluateFormat;
    private evaluateLogLevel;
    private getEnvValues;
    private chooseMostVerboseLevel;
    private evaluateMetadata;
    private removeTagFromMetadata;
}
